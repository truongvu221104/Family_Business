<Window x:Class="Family_Business.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Family_Business.Views"
        mc:Ignorable="d"
        Title="Đăng nhập" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Username:"/>
        <TextBox x:Name="txtUser" Margin="0,4" Grid.Row="1"/>

        <TextBlock Text="Password:" Grid.Row="2" Margin="0,10,0,0"/>
        <PasswordBox x:Name="txtPass" Margin="0,4" Grid.Row="3"/>

        <Button Content="Login" Width="80" Grid.Row="4" 
            HorizontalAlignment="Right" Margin="0,10,0,0"
            Click="Login_Click"/>
    </Grid>
</Window>
