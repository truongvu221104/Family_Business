<!-- DebtOverviewView.xaml -->
<UserControl x:Class="Family_Business.Views.DebtOverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Filter panel -->
            <RowDefinition Height="*"/>
            <!-- DataGrid -->
            <RowDefinition Height="Auto"/>
            <!-- Payment panel -->
        </Grid.RowDefinitions>

        <!-- 0) Filter panel -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10" VerticalAlignment="Center">
            <TextBlock Text="Tìm kiếm (Tên/SĐT):" VerticalAlignment="Center"/>
            <TextBox x:Name="txtFilterNameOrPhone" Width="200" Margin="5,0"
                     TextChanged="Filter_Changed"/>
            <TextBlock Text="Ngày đáo hạn từ:" Margin="20,0,0,0" VerticalAlignment="Center"/>
            <DatePicker x:Name="dpFilterDueDate" Margin="5,0"
                        SelectedDateChanged="Filter_Changed"/>
            <Button Content="Xóa lọc" Margin="20,0,0,0" Click="BtnClearFilter_Click"/>
        </StackPanel>

        <!-- 1) Bảng khách còn nợ -->
        <DataGrid x:Name="dgDebtors"
          Grid.Row="1"
          AutoGenerateColumns="False"
          SelectionMode="Single"
          SelectionChanged="DgDebtors_SelectionChanged"
          IsReadOnly="True">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsOverdue}" Value="True">
                            <Setter Property="Background" Value="Salmon"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsHighlight}" Value="True">
                            <Setter Property="Background" Value="LightGoldenrodYellow"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Khách hàng" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Điện thoại" Binding="{Binding PhoneNumber}" Width="150"/>
                <DataGridTextColumn Header="Địa chỉ" Binding="{Binding Address}" Width="*"/>
                <DataGridTextColumn Header="Số nợ" Binding="{Binding Balance, StringFormat=N2}" Width="120"/>
                <DataGridTextColumn Header="Ngày đáo hạn" Binding="{Binding NextDueDate, StringFormat=yyyy-MM-dd}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 2) Panel thanh toán -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock Text="Số tiền cần trả:" VerticalAlignment="Center"/>
            <TextBox x:Name="tbOwed" Width="120" Margin="5,0" IsReadOnly="True"/>

            <TextBlock Text="Nhập số tiền trả:" Margin="20,0,0,0" VerticalAlignment="Center"/>
            <TextBox x:Name="tbPayment" Width="120" Margin="5,0"/>

            <Button x:Name="btnPay" Content="Thanh toán" Width="100" Margin="20,0,0,0" Click="BtnPay_Click"/>
        </StackPanel>
    </Grid>
</UserControl>